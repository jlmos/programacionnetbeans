/* explicar framentos de código
autor: jose luis mosquera losada
fecha: 10-11-2021
 */
package JLMLt04;


public class JLMLt04e01cuestiones {

    public static void main(String[] args) {
        
  /* a)  
        if(cadena.substring(0,2).equals(cadena.substring(cadena.length()-2, cadena.length()))
        
     No entiendo bien este código: se que quiere comparar dos trozos de la cadena
  pero no entiendo para que.
  
     b)
       b=false;
     for (int i=0,i<=cad.length()-2;i++)
         if (cad.charAt(i)==cad.charAt(i+1)) b=true;
  
  Este código coge desde cero hasta el antepenultimo carácter y quiere esa posición
  Con length() haría toda la cadena y con length ()-1 haría hasta la última posición
  sin incluirla que creo que estos dos últimos length hacen lo mismo porque no se 
  tiene en cuenta, para contar, la última posición
  
      c) 
  res = nom.toUpperCase().charAt(0) +"." + ape1.toUpperCase().charAt(0)+"." +
ape2.toUpperCase().charAt(0) +"."; 
  
  En este código pone la primera letra en mayusculas del nombre, 1apellido y 2do apellido
  que puede estar ya dado y haríamos una concatenación o introducido por pantalla y corregiríamos
  el error sacandolo por consola.*/
     
    
    
          }}
            
            
        

    

